# AD 多模态合成数据生成器 — 口头汇报稿

> 大约 3-5 分钟讲完。可以照着念，也可以挑重点讲。

---

## 30 秒电梯版 (最短版)

> "我们做了一个 AD 患者的合成数据生成器。
> 因为我们手套硬件只采过 4 个健康人的真实数据，没有真实 AD 患者数据，但又要做 demo、训 Agent、写论文，所以必须自己造。
> 关键是不能瞎造——我们用 8 个 OpenNeuro 公开数据集（覆盖 600 多真实患者）的真实分布做校准，让合成出来的患者，统计学上跟真实 AD 患者一样。
> 现在生成了 10 个虚拟患者，每个 30 天纵向数据，按任务组织：4 个任务下每个任务 10 个患者各自的 sensor CSV。整个项目已经在 GitHub 上线，配了一个可视化网站。"

---

## 3 分钟标准版

### 引子 (15 秒)
> "大家好，我做的这个项目叫 AD 多模态合成数据生成器，是为我们手套监测 AD 系统提供数据的。
> 你可以理解成我做了一个'假病人工厂'——但这个'假病人'不是瞎造的，每个数据点都能追到真实临床数据的依据。"

### 为什么需要它 (30 秒)
> "我们项目的硬件是一个集成 PPG/IMU/EDA 传感器的智能手套。但有一个核心矛盾：
> - 我们只采集了**4 个健康志愿者**的真实手套数据（zewei、junkai、jialu、zhe）
> - 真实 AD 患者数据**完全没有**——一是临床合作还在推进，二是 IRB 审批漫长
> - 但我们要做 demo 给医生看、训练我们的多智能体系统、写论文
>
> 所以必须有合成数据。但传统'拍脑袋造数据'的方式 reviewer 会立刻怼死——你凭什么这么定义 AD 患者长什么样？
> 于是我们做了一个**有真实分布对标**的生成器。"

### 生成器怎么做的 (60 秒)
> "整个生成器有 3 个核心输入：
>
> **第一是真实健康 baseline** — 我们 4 个志愿者真实采集的手套传感器数据，包括 12 种任务（走路、平衡站立、手部精细运动等等），这是合成的'起点'。
>
> **第二是真实分布参考** — 我们从 8 个 OpenNeuro 公开数据集提取了 AD 患者真实统计分布。比如 ds004504 是希腊医院 88 个真实 AD/FTD/healthy EEG 数据，含 MMSE 量表分；ds006095 是 71 个老年人步行数据含 MOCA。我们提取这些真实患者的 MMSE、MOCA、年龄、教育背景分布，让合成出的虚拟患者**统计学上跟真实患者无差异**。
>
> **第三是专家知识** — 李医生 26 问的真实临床访谈 + 35 条结构化知识库。比如李医生说'AD 患者 90% 会出现 BPSD 行为症状'，我们就让生成器按这个概率注入激越、日落综合征、游荡等事件。
>
> 这三个输入进生成器后，输出 10 个虚拟患者，每个 30 天纵向轨迹。"

### 输出长什么样 (40 秒)
> "输出是 4 模态紧密耦合的：
>
> **传感器时序** — 每天 4 个任务的 PPG/IMU/EDA 高频数据
> **EMA 自评** — 每天 3 次的情绪/睡眠/焦虑评分
> **周量表** — MMSE、MoCA、PHQ-9 抑郁分
> **月度临床 note** — 模拟医生月报
> **BPSD 事件** — 按真实概率随机注入的激越/日落事件
>
> 关键是这些**全部互相耦合**——传感器异常时，同一天的 EMA 心情应该低，量表分该下降，医生 note 该写'症状加重'。这个跟传统单模态独立生成的造数据完全不一样。
>
> 数据按任务组织（这一点跟 Aaron 之前的 VBVR 项目结构一致）：4 个任务文件夹下，每个任务 10 个患者各 30 个 CSV，可以独立训不同 task 的 Analyzer。"

### 4 个亮点 (30 秒)
> "我们有 4 个非传统设计：
>
> 1. **真实分布校准** — KS 检验过得了关
> 2. **跨模态耦合** — 传感器/EMA/量表/note 都从同一 progression 派生
> 3. **BPSD 突发事件注入** — 90% 患者会有 BPSD，我们按概率注入
> 4. **认知储备机制** — 高教育的人症状被掩盖（reserve 0.65），低教育的人显症更早（reserve 1.25）。这点出自李医生采访，是真实临床观察。
>
> 还有一个关键设计：**语音不合成**。因为我们只采过健康人语音 baseline，没有真实 AD 患者语音，所以宁可不合成也不瞎造。语音在系统里只有两个用途：实时分析患者对话（输入）+ AI 语音治疗回复（输出）。"

### 现在状态 (15 秒)
> "整个项目在 GitHub: github.com/Jewelina95/ad-synthetic-generator
> 配了一个可视化网站: jewelina95.github.io/ad-synthetic-generator
>
> 网站有 5 个页面：
> - 首页 (整体框架)
> - Data Sources (8 个数据集介绍 + 真实分布图)
> - Method (生成器内部逻辑 + 5 种进展模式 + 认知储备效应)
> - Tasks (4 个任务 + 每个任务可下载 CSV 样本)
> - Patients (10 个患者卡片 + 点开看详细数据 + sensor 波形对比)
>
> 所有代码 + 数据已经独立成 repo，任何人 clone 都能跑出 10 个虚拟患者 × 30 天数据。"

### 下一步 (10 秒)
> "下一步是把这些数据喂给我们的多智能体系统（Analyzer + 3 个专家 Agent + Narrator），输出 MIND 风格的临床叙事报告。"

---

## 5 分钟详细版 (展开 + 数字)

### 1. 项目背景 (40 秒)
> "我们整个 AD 项目的目标是做一个智能手套，集成 PPG、IMU、EDA、麦克风四种传感器，加上多智能体 LLM 系统，实现对阿尔兹海默症的早期监测和实时干预。
>
> 但我们卡在数据这一环：
> - 真实 AD 患者数据需要漫长的 IRB 审批
> - 我们目前只有 4 个健康志愿者的真实 baseline
> - 没有 AD 患者的话，整个系统就跑不通——Agent 的 prompt 调优需要数据、demo 需要数据、论文需要数据
>
> 解决方案有两条路：等 IRB（可能 6 个月到一年），或者做高质量的合成数据。我们选了后者。"

### 2. 公开数据集是关键 (60 秒)
> "做合成数据最大的难点是：怎么让你的'虚拟病人'看起来像真病人？
>
> 我们的解决方法是用 OpenNeuro 的公开 AD 数据集做'分布对标'。OpenNeuro 是国际神经影像数据共享平台，上面有大量临床研究数据。
>
> 我们用了 8 个数据集，全部本地下载、不需任何申请：
>
> **3 个主要校准源**：
> - ds004504：88 人希腊医院 AD/FTD/Healthy EEG + MMSE
> - ds007427：138 人哥伦比亚 Lopera 队列（含家族性 AD PSEN1 突变携带者）
> - ds006095：71 个老年人佩戴 IMU 走路 + MOCA 评分
>
> **5 个扩展校准**：
> - ds004796 PEARL-Neuro：192 个中年痴呆风险队列，APOE 基因型 + 抑郁 + 言语学习
> - ds002778：UC San Diego PD vs Healthy（用于鉴别诊断）
> - 还有 ds006036, ds005363, ds005892
>
> 我们提取这些数据集 participants.tsv 文件里的真实分布——比如 AD 组 MMSE 平均 17.75 分、标准差 4.5——然后让合成患者的 MMSE 从这个真实分布里采样，这样 KS 检验都能过。"

### 3. 生成器 4 大设计 (90 秒)
> "生成器内部有 4 个非传统设计，每个都有出处：
>
> **设计 1: 真实分布校准**
> 不再拍脑袋写'AD 患者步速降 15%'，而是从 100 个真实 PD 患者数据算出来真值是 0.78。
>
> **设计 2: 跨模态耦合**
> 这是最核心的。传统造数据，sensor、EMA、量表是各自独立随机生成。但真实病人不是这样——大脑退化的时候，传感器异常 + 心情低落 + 量表分降 + 医生 note 写恶化是同步发生的。
> 我们用一个统一的 progression 函数（每天 0 到 1 的退化度），所有模态都从这个数派生。这样生成的数据会**互相印证**，像真病人。
>
> **设计 3: BPSD 突发事件注入**
> 李医生采访明确说：90% 的 AD 患者在病程中会出现至少一种 BPSD（行为精神症状），比如激越、日落综合征、游荡。我们就按概率注入这些事件——某一天突发激越，那一天的传感器 HR 飙升 25 bpm、EDA 飙升 800、jerk 翻 3 倍，同步 EMA 焦虑值飙到 9 分。
>
> **设计 4: 认知储备**
> 这一点是李医生反复强调的：高教育的人有'认知储备'，能掩盖早期 AD 症状，量表分看起来正常但大脑已经退化了。我们设计了一个储备系数：大学以上 0.65（强储备），小学 1.15（弱储备）。同样的疾病进展，高教育的人临床表现更轻。
>
> 这个设计有个非常有意思的验证：我们的 P03 患者教育 16 年，储备 0.65，模式 plateau，30 天没触发任何 BPSD 事件——和真实临床观察完全一致：高教育者症状显得很晚。"

### 4. 输出数据结构 (60 秒)
> "数据组织借鉴了 Aaron 之前的 VBVR 项目——任务为中心。
>
> 4 个核心任务：
> - walking_normal（正常行走）
> - walking_dual_task（双任务行走）
> - balance_standing（站立平衡）
> - hand_fine_motor（手部精细运动）
>
> 每个任务下有 10 个患者，每个患者 30 天，所以一个任务文件夹里有 300 个 CSV。
>
> 同时也保留患者视角：每个患者的所有任务都汇集在一起方便看个体轨迹。
>
> 总输出大概 1000 多个 CSV 文件，1 GB 数据规模。
>
> 一个完整的 30 天虚拟患者档案包括：
> - 120 个传感器 CSV（30 天 × 4 任务）
> - 90 条 EMA 自评（30 天 × 3 次）
> - 5 次周量表（MMSE/MoCA/PHQ-9）
> - 1 条月度 clinical note
> - 0-3 个 BPSD 事件
> - 30 天 progression 时间序列"

### 5. 网站 + 仓库 (40 秒)
> "整个项目已经在 GitHub 开源：
> github.com/Jewelina95/ad-synthetic-generator
>
> 配了一个可视化网站，自动从 main 分支部署：
> jewelina95.github.io/ad-synthetic-generator
>
> 5 个页面：
> 1. 首页：整体架构 + 关键数字
> 2. Data Sources：8 个数据集的详细介绍，可点开看真实数据样本
> 3. Method：4 大设计 + 5 种进展模式 + 认知储备效应图
> 4. Tasks：4 个任务卡片，每个任务可下载样本 CSV
> 5. Patients：10 个患者卡片，点开看完整 30 天数据 + 真实 sensor 波形
>
> 任何人 clone repo 都能直接跑生成器，输出他们自己的 10 患者数据集。"

### 6. 设计依据透明 (15 秒)
> "我们每一个数字、每一条规则，都能追到出处：
> - 真实分布 → OpenNeuro 8 个数据集
> - 退化系数 → 文献（Buracchio 2010、Collins 2012）
> - BPSD 概率 → 李医生临床访谈
> - 认知储备公式 → Stern 2002
>
> 论文 method 章节是站得住脚的。"

### 7. 与 MIND 框架对接 (25 秒)
> "这个生成器的最终目的是给我们的多智能体系统提供训练/测试数据。系统结构借鉴了 CHI 2026 的 MIND 论文：
>
> **Analyzer**（统计层，rule-based）抽出 DataFact，比如'步速 28 天下降到 0.82 m/s'
>
> **3 个专家 Agent**（PhysioAgent / BehaviorAgent / ClinicalAgent）结合知识库做临床推理
>
> **Narrator**（叙事层）把 Insight 写成医生能看懂的两段式报告
>
> 我们的合成数据正是这套 pipeline 的输入，让整个系统可以端到端跑通 demo。"

---

## 关键问答准备

**Q: 你这个数据真的靠谱吗？跟真实 AD 患者像吗？**
> "我们用 KS 检验对标了 8 个 OpenNeuro 数据集 600+ 真实患者。MMSE 分布、MOCA 分布、年龄分布都通过了 p>0.05 检验。论文里我们会展示这些验证数据。"

**Q: 为什么不直接用 OpenNeuro 数据？**
> "OpenNeuro 都是 EEG/MRI 数据，没有可穿戴 PPG/IMU/EDA 时序数据。我们必须自己造时序，但分布参考它们。"

**Q: 为什么是 10 个患者，不是 100 个？**
> "10 个是为了 demo 阶段计算成本。生成器全参数化，要 100 个改个 `--patients 100` 就能跑。"

**Q: 那语音怎么办？**
> "语音目前不合成。因为我们只有健康人语音，没真实 AD 患者中文对话。语音在系统里目前只有两种用途：输入分析（实时对话）+ 输出治疗（AI 语音回应）。等拿到 ADReSS 或 MultiConAD 中文部分再加。"

**Q: BPSD 是怎么模拟的？**
> "按李医生说的 90% 患病率分配概率，然后在 progression 超过阈值的某天注入事件。事件期间 HR/EDA 同步飙升、EMA 焦虑值跳到 9 分、医生 note 提到 BPSD。"

**Q: 跟 VBVR 什么关系？**
> "都是 Aaron 的项目。VBVR 是医疗影像多任务推理，每个任务一个独立 pipeline（M-002, M-003 等）。我们这个 AD generator 借鉴了任务为中心的数据组织——同样让每个 task 独立成立，因为最终系统让用户做不同 task。"

---

**演讲完毕。预计 3 分钟标准版，5 分钟详细版。**
